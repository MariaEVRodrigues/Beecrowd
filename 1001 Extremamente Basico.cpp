#include <stdio.h>

int main (){
	int a, b, x; 
	
	scanf ("%d %d", &a, &b);
	
	x = a + b; //x armazenará a soma.
	
	printf ("X = %d\n", x); //impressão da soma na tela conforme saída do enunciado.
	
	return 0;
}
